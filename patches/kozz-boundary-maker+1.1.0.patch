diff --git a/node_modules/kozz-boundary-maker/dist/InlineCommand/parser.js b/node_modules/kozz-boundary-maker/dist/InlineCommand/parser.js
index 56bd025..14fe97d 100644
--- a/node_modules/kozz-boundary-maker/dist/InlineCommand/parser.js
+++ b/node_modules/kozz-boundary-maker/dist/InlineCommand/parser.js
@@ -25,25 +25,103 @@ var __importStar = (this && this.__importStar) || function (mod) {
 Object.defineProperty(exports, "__esModule", { value: true });
 exports.parseMessageBody = void 0;
 const T = __importStar(require("@guigalleta/t-parser"));
-const commandParser = T.transform(T.sequenceOf([
-    T.str('%', 'Command Starter'),
-    T.regexMatch(/^[^:]+/, 'Command Name'),
-    T.str(':', 'Command name-payload divider'),
-    T.regexMatch(/^[^}]+/, 'Command payload'),
-    T.str('}'),
-], 'Command'), ({ result }) => ({
-    type: 'command',
-    commandName: result[1],
-    commandData: JSON.parse(result[3] + result[4]),
-}));
-const stringParser = T.transform(T.regexMatch(/^[^%]+/, 'Any text'), ({ result }) => ({
-    type: 'string',
-    value: result,
-}));
-const parser = T.atLeastOne(T.choice([stringParser, commandParser]));
+const readJsonPayload = (input, start) => {
+    if (input[start] !== '{') {
+        return null;
+    }
+    let depth = 0;
+    let inString = false;
+    let escaped = false;
+    for (let i = start; i < input.length; i++) {
+        const char = input[i];
+        if (escaped) {
+            escaped = false;
+            continue;
+        }
+        if (char === '\\') {
+            escaped = true;
+            continue;
+        }
+        if (char === '"') {
+            inString = !inString;
+            continue;
+        }
+        if (inString) {
+            continue;
+        }
+        if (char === '{') {
+            depth++;
+        }
+        if (char === '}') {
+            depth--;
+            if (depth === 0) {
+                return input.slice(start, i + 1);
+            }
+        }
+    }
+    return null;
+};
+const parseInlineCommandAt = (input, start) => {
+    const match = /^%([^:%]+):/.exec(input.slice(start));
+    if (!match) {
+        return null;
+    }
+    const payloadStart = start + match[0].length;
+    const payload = readJsonPayload(input, payloadStart);
+    if (!payload) {
+        return null;
+    }
+    try {
+        return {
+            item: {
+                type: 'command',
+                commandName: match[1],
+                commandData: JSON.parse(payload),
+            },
+            nextIndex: payloadStart + payload.length,
+        };
+    }
+    catch (_a) {
+        return null;
+    }
+};
+const parser = {
+    run: (input) => {
+        const result = [];
+        let stringBuffer = '';
+        for (let i = 0; i < input.length;) {
+            const command = input[i] === '%' ? parseInlineCommandAt(input, i) : null;
+            if (command) {
+                if (stringBuffer) {
+                    result.push({
+                        type: 'string',
+                        value: stringBuffer,
+                    });
+                    stringBuffer = '';
+                }
+                result.push(command.item);
+                i = command.nextIndex;
+                continue;
+            }
+            stringBuffer += input[i];
+            i++;
+        }
+        if (stringBuffer) {
+            result.push({
+                type: 'string',
+                value: stringBuffer,
+            });
+        }
+        return {
+            isError: false,
+            result,
+            errorStack: [],
+            error: '',
+        };
+    },
+};
 const parseMessageBody = (string) => {
-    const { isError, result, errorStack, error } = T.parse(string, parser);
-    console.log({ result, errorStack, error });
+    const { isError, result } = parser.run(string);
     if (isError) {
         return [
             {
diff --git a/node_modules/kozz-boundary-maker/dist/index.d.ts b/node_modules/kozz-boundary-maker/dist/index.d.ts
index f91b045..606a0e5 100644
--- a/node_modules/kozz-boundary-maker/dist/index.d.ts
+++ b/node_modules/kozz-boundary-maker/dist/index.d.ts
@@ -5,6 +5,7 @@ type InitOptions = {
     socketPath?: string;
     chatPlatform: Platform;
     name: string;
+    namespace?: string;
     inlineCommandMap?: Partial<InlineCommandMap>;
 };
 type EventPayload = {
@@ -24,7 +25,7 @@ type EventPayload = {
 type EvCallback = {
     [evName in keyof EventPayload]: (payload: EventPayload[evName]) => any;
 };
-declare const initBoundary: ({ url, socketPath, chatPlatform, name, inlineCommandMap, }: InitOptions) => {
+declare const initBoundary: ({ url, socketPath, chatPlatform, name, namespace, inlineCommandMap, }: InitOptions) => {
     kozzSocket: import("socket.io-client").Socket<import("@socket.io/component-emitter").DefaultEventsMap, import("@socket.io/component-emitter").DefaultEventsMap>;
     on: <EvName extends keyof EventPayload>(evName: EvName, handler: EvCallback[EvName]) => void;
     onAskResource: (resourceName: string, getterFn: (data: any) => any) => void;
diff --git a/node_modules/kozz-boundary-maker/dist/index.js b/node_modules/kozz-boundary-maker/dist/index.js
index 91153d5..a404d98 100644
--- a/node_modules/kozz-boundary-maker/dist/index.js
+++ b/node_modules/kozz-boundary-maker/dist/index.js
@@ -4,7 +4,7 @@ const socket_io_client_1 = require("socket.io-client");
 const PayloadSign_1 = require("./util/PayloadSign");
 const InlineCommand_1 = require("./InlineCommand");
 const parser_1 = require("./InlineCommand/parser");
-const initBoundary = ({ url, socketPath, chatPlatform, name, inlineCommandMap, }) => {
+const initBoundary = ({ url, socketPath, chatPlatform, name, namespace = process.env.KOZZ_NAMESPACE || process.env.NAMESPACE || 'default', inlineCommandMap, }) => {
     const kozzSocket = (0, socket_io_client_1.io)(url, {
         path: socketPath !== null && socketPath !== void 0 ? socketPath : '/socket.io/',
     });
@@ -15,6 +15,7 @@ const initBoundary = ({ url, socketPath, chatPlatform, name, inlineCommandMap, }
             platform: chatPlatform,
             role: 'boundary',
             name,
+            namespace,
         };
         kozzSocket.emit('introduction', (0, PayloadSign_1.signPayload)(payload));
     });
